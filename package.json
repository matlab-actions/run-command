{
    "name": "run-matlab-command-action",
    "author": "The MathWorks, Inc.",
    "version": "0.0.1",
    "description": "",
    "main": "lib/index.js",
    "scripts": {
        "clean": "rm -rf dist lib",
        "format": "prettier --write .",
        "format-check": "prettier --check .",
        "build": "tsc",
        "deps": "bash ./scripts/setupdeps.sh",
        "package": "ncc build --minify",
        "test": "jest",
        "all": "npm test && npm run build && npm run package",
        "ci": "npm run clean && npm run deps && npm run ci && npm run all"
    },
    "files": [
        "lib/"
    ],
    "dependencies": {
        "@actions/core": "^1.2.4",
        "@actions/exec": "^1.0.4",
        "uuid": "^8.3.0"
    },
    "devDependencies": {
        "@types/jest": "^26.0.9",
        "@types/node": "^14.0.27",
        "@types/uuid": "^8.3.0",
        "@zeit/ncc": "^0.22.3",
        "jest": "^26.3.0",
        "jest-circus": "^26.3.0",
        "prettier": "2.0.5",
        "ts-jest": "^26.2.0",
        "typescript": "^3.9.7"
    }
}
