{
    "name": "run-matlab-command-action",
    "author": "The MathWorks, Inc.",
    "version": "3.0.1",
    "description": "",
    "type": "module",
    "main": "lib/index.js",
    "scripts": {
        "clean": "rm -rf dist lib",
        "format": "prettier --write .",
        "format-check": "prettier --check .",
        "build": "tsc",
        "deps": "bash ./scripts/setupdeps.sh",
        "package": "ncc build --minify",
        "test": "NODE_OPTIONS='--experimental-vm-modules' jest --passWithNoTests",
        "all": "npm run format-check && npm test && npm run build && npm run package",
        "ci": "npm run clean && npm ci && npm run deps && npm run all"
    },
    "files": [
        "lib/"
    ],
    "dependencies": {
        "@actions/core": "^3.0.0",
        "@actions/exec": "^3.0.0",
        "common-utils": "github:matlab-actions/common-utils#kapilg/multiple-session"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "@types/node": "^24.3.0",
        "@vercel/ncc": "^0.38.3",
        "jest": "^30.0.5",
        "jest-circus": "^30.0.5",
        "prettier": "3.6.2",
        "ts-jest": "^29.4.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.2"
    }
}
